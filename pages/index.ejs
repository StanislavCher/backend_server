<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
</head>
<body>
<% if (created) { %>
    <div class="alert alert-success">
        Note has been created!
    </div>
<% } %>
<div class="container">
    <h1><%= title %></h1>
    <form action="/" method="post" class="mb-2">
        <div class="form-group mb-2">
            <label for="text">Text</label>
            <input type="text" name="title" id="text" class="form-control">
        </div>
        <button type="submit" class="btn btn-primary">Create!</button>
    </form>
    <% if (notes.length) { %>
    <div class="card">
        <ul class="list-group">
            <% for (let i = 0; i < notes.length; i++) { %>
                <li class="list-group-item d-flex justify-content-between align-items-center">
                    <div id="note-name" class="justify-content-left">
                        <span><%= notes[i].title %></span>
                    </div>
                    <div class="justify-content-right">
                        <button type="submit" class="btn btn-primary" data-type="edit" data-id="<%= notes[i].id %>" data-title="<%= notes[i].title %>">Редактировать</button>
                        <button type="submit" class="btn btn-danger" data-type="remove" data-id="<%= notes[i].id %>">&times;</button>
                    </div>
                </li>
            <% } %>
        </ul>
    </div>
    <% } else { %>
        <p>No notes!</p>
    <% } %>
</div>
<!--<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>-->
<script src="app.js"></script>
</body>
</html>